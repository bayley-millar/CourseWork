/**figure skating.c
 *Bayley Millar
 *Lab2
 */
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main(void){
    double s1;
    double s2;
    double s3;
    printf("Please input the scores");
    fflush(stdout);
    scanf("%lg %lg %lg", &s1, &s2, &s3);
    if(s1 < s2 && s1 < s3){
        printf("%f", (s2 + s3)/2);
    }else if (s2 < s1 && s2 < s3){
        printf("%f", (s1 + s3)/2);
    }else{
        printf("%f", (s1 + s2)/2);
    }
    return EXIT_SUCCESS;
}
    
    
    
