1.the local port number to which this socket is
bound or -1 if the socket is not bound yet.

2.the remote IP address to which this socket is connected,
or null if the socket is not connected.

3.the remote port number to which this socket is connected,
or 0 if the socket is not connected yet.